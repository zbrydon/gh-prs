# gh-prs

View pull requests you have created:

```sh
gh prs
gh prs --drafts
```

View pull requests that have requested your review:

```sh
gh prs --req
gh prs --req --drafts
```

Install:

```sh
gh extension install zbrydon/gh-prs
```
